#include "main.h"
/**
 * _abs - function that compute the absolate value of an integer
 *@n: character checker
 *
 * Return: abs of num
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-1 * n);
}
